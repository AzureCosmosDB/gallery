// Bridge file to ensure imports like `@theme/Navbar/Content` resolve
// from environments that expect a .js entrypoint.
// eslint-disable-next-line import/no-unused-modules
export { default } from './index.tsx';
